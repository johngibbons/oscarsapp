<p id="notice"><%= notice %></p>

<%= render 'form' %>

<h1>Oscar Categories</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody id="categories">
      <%= render @categories %>
  </tbody>
</table>

<br>

<%= link_to 'New Category', new_category_path %>
