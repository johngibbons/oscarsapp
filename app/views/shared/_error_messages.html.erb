<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="flash-error">
      The form contains <%= pluralize(object.errors.count, "error") %>.
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li class="individual-error"><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>